services:
  browser-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - BROWSER_HEADLESS=true
    volumes:
      - ./downloads:/app/downloads
    restart: unless-stopped
